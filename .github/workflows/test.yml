name: Test

on:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v1
        with:
          node-version: 12
        env:
          - CI: false
      - run: yarn install
      - run: yarn build
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Implementar teste"
