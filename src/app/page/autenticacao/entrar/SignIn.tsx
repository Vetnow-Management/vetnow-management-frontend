import React, { ReactElement } from 'react';

import { Typography } from '@material-ui/core';
import { CreateGrid } from '../../../component';

export default function SignIn(): ReactElement {

  return (
    <CreateGrid>
      <Typography>SIGN IN</Typography>
    </CreateGrid>
  )
}
